<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tasky_20May" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/statusbar</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logowhite</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Tasky_20May</item>
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="CustomTab" parent="Widget.AppCompat.ActionBar.TabBar">
        <item name="tabIconTint">@color/black</item>
        <item name="tabIndicatorColor">@color/black</item>
    </style>

    <style name="Theme.Tasky_20May" parent="Base.Theme.Tasky_20May" />
</resources>